# 시그널데스크(SignalDesk) 서비스 기획서

- 문서 버전: v0.1
- 작성일: 2026-08-16
- 대상: 1인 개발(앱·웹·서버 전담)
- 산출물: 본 기획서 + 화면 스토리보드(HTML)

---

## 1. 서비스 개요

### 1.1 한 줄 정의
종목 하나를 검색하면, **"지금 이 종목은 매수 구간인가 매도 구간인가"** 를 기술적 지표와 매크로 지표를 합산한 단일 점수로 알려주는 웹서비스.

### 1.2 해결하는 문제
| 문제 | 현재 상황 | 시그널데스크의 해법 |
|---|---|---|
| 지표가 흩어져 있음 | 볼린저밴드, 엔벨로프, 거래량, VIX를 각각 다른 화면에서 확인 | 한 화면에 통합 |
| 해석을 못 함 | 지표는 보이는데 "그래서 사라는 거야 말라는 거야"가 없음 | 지표 → 점수 → 5단계 구간 판정 |
| 매크로가 빠짐 | 개별 종목만 보고 시장 전체 분위기를 무시 | VIX/VKOSPI·금리·환율을 점수에 반영 |

### 1.3 타깃 사용자
- 1차: 단기~중기 스윙 투자자. 종목은 알지만 진입/청산 타이밍 판단에 자신이 없는 사람.
- 유니버스: **KOSPI200, 코스닥150, NASDAQ100, S&P500** (약 700종목, 중복 제거 후)
- 다우존스는 S&P500에 대부분 포함되므로 제외.

### 1.4 차별점
- "정보 나열"이 아니라 **"단일 판정"** 을 최상단에 배치.
- 판정 근거를 지표별로 펼쳐볼 수 있어 블랙박스가 아님.
- 종가 기준 1일 1회 갱신 → 실시간 매매 툴이 아닌 **"매일 아침 확인하는 체크리스트"** 포지셔닝.

---

## 2. 개발 범위 (단계별)

### 2.1 1차 개발 — MVP (오픈 목표)
> 목표: **로그인 없이, 검색하면 판정이 나온다.** 이것만 되면 오픈.

| # | 기능 | 필수 여부 |
|---|---|---|
| F-01 | 종목 검색(자동완성) | 필수 |
| F-02 | 종목 상세 — 종목명·현재가·등락률 | 필수 |
| F-03 | **분석 결과 카드(시그널 게이지)** — 차트 상단, 현재가 바로 아래 | 필수 |
| F-04 | 캔들 차트 + 볼린저밴드 + 엔벨로프 + 거래량 | 필수 |
| F-05 | 공포지수(VIX / VKOSPI) 표시 | 필수 |
| F-06 | 지표 상세 패널(RSI, MACD, 이동평균, %B 등) | 필수 |
| F-07 | 즐겨찾기 — **localStorage 기반, 로그인 없음** | 필수 |
| F-08 | 면책 고지 상시 노출 | 필수 |

**1차에서 의도적으로 빼는 것**
- 회원가입/로그인 (즐겨찾기는 브라우저 저장으로 대체)
- 실시간 시세 (종가 기준 일 1회 배치)
- 결제, 알림, 백테스트, 포트폴리오
- 앱(React Native) — 웹 반응형으로 커버

### 2.2 2차 개발 — 수익화
| # | 기능 |
|---|---|
| F-09 | 회원가입/로그인 (이메일 + 소셜) |
| F-10 | localStorage 즐겨찾기 → 계정 이관 |
| F-11 | **일일 조회 한도(5종목) 카운팅 + 페이월** |
| F-12 | 요금제 화면 + 결제 연동(정기결제) |
| F-13 | 구독 관리(해지/영수증) |
| F-14 | 관리자 대시보드(조회 로그, 전환율) |

### 2.3 3차 개발 — 리텐션
| # | 기능 |
|---|---|
| F-15 | 시그널 전환 알림(중립→매수 등) — 이메일/웹푸시 |
| F-16 | 백테스트 리포트("이 시그널을 따랐다면") |
| F-17 | 관심종목 요약 리포트(매일 아침 발송) |
| F-18 | 스크리너(전체 유니버스에서 매수 구간 종목 리스트) |
| F-19 | React Native 앱 (웹뷰 하이브리드 → 점진 네이티브화) |

---

## 3. 화면 목록 (IA)

```
/                     S-01  홈 / 검색                        [1차]
                      S-02  검색 자동완성 드롭다운           [1차]
/stock/:ticker        S-03  종목 상세 (헤더+시그널)          [1차]
                      S-04  종목 상세 (차트)                 [1차]
                      S-05  종목 상세 (지표/매크로)          [1차]
/favorites            S-06  즐겨찾기                          [1차]
                      S-07  상태 화면(로딩/빈/에러)          [1차]
                      S-08  조회 한도 경고 배너              [2차]
                      S-09  페이월 모달                      [2차]
/pricing              S-10  요금제                            [2차]
/login                S-11  로그인 / 회원가입                [2차]
/alerts               S-12  알림 설정                        [3차]
/screener             S-13  스크리너                          [3차]
```

---

## 4. 핵심 기능 명세

### 4.1 F-01 종목 검색
- 입력: 종목명(한글/영문), 티커, 종목코드
- 동작: 2글자 이상 입력 시 300ms 디바운스 후 자동완성 호출
- 결과: 최대 8건. 시장 배지(KOSPI/KOSDAQ/NASDAQ/NYSE) + 현재가 + 등락률 동시 표시
- 검색 대상은 유니버스 내 종목으로 한정. 미포함 종목 검색 시 "지원 예정 종목" 안내
- 키보드 조작: ↑↓ 이동, Enter 선택, Esc 닫기

### 4.2 F-03 분석 결과 카드 (최우선 요소)
**배치 규칙: 종목명 → 현재가 → 분석 결과 카드 → 차트 순서.** 차트보다 위에 위치.

카드 구성:
1. **시그널 게이지** — -100 ~ +100 수평 눈금자에 현재 점수 표시
2. **구간 라벨** — 5단계 중 하나
3. **한 줄 요약** — "20일선 위 정배열, 거래량 동반 상승. 다만 VIX 상승으로 시장 부담."
4. **근거 태그 3~5개** — `정배열` `%B 0.82` `거래량 2.1배` `VIX↑`
5. **면책 문구** — "투자 판단의 참고 정보이며 투자 권유가 아닙니다."

### 4.3 F-04 차트
- 라이브러리: **lightweight-charts** (TradingView, Apache-2.0)
- 기본 기간: 6개월(일봉). 토글: 3M / 6M / 1Y / 3Y
- 오버레이 (토글 가능):
  - 볼린저밴드 (20, 2σ) — 상단/중심/하단 3라인 + 밴드 영역 음영
  - 엔벨로프 (MA20 ±10%) — 점선 2라인
  - 이동평균 20 / 60 / 120
- 하단 서브차트: 거래량 막대 (20일 평균선 오버레이)
- 색상 규약: **상승 = 레드, 하락 = 블루** (국내 관례)

### 4.4 F-07 즐겨찾기
- **1차**: `localStorage` 키 `sd.favorites` 에 `[{ticker, market, addedAt}]` 저장. 최대 20종목.
- 상세 화면 헤더 우측 별 아이콘 토글
- `/favorites` 에서 카드 리스트로 표시, 각 카드에 미니 시그널 게이지 + 등락률
- **2차**: 로그인 시 localStorage 내용을 서버로 1회 병합(merge) 후 서버 기준으로 전환

### 4.5 F-11 조회 한도 & 페이월 (2차)
- 무료: **하루 5종목 상세 조회** (동일 종목 재조회는 24시간 내 1회로 카운트)
- 3번째 조회부터 헤더에 잔여 카운트 배너 노출
- 6번째 시도 시 페이월 모달 → 콘텐츠는 블러 처리, 시그널 게이지만 흐리게 티저
- 카운팅 기준:
  - 비로그인: `deviceId(UUID, localStorage) + IP 해시` — 우회 가능함을 전제, 로그인 유도 목적
  - 로그인: `user_id` 기준 (정확)
  - 리셋: KST 자정
- 서버에서 원자적으로 증가(`INSERT ... ON CONFLICT DO UPDATE SET count = count + 1 RETURNING count`) — 클라이언트 카운트는 표시용일 뿐 신뢰하지 않음

---

## 5. 분석 엔진 설계

### 5.1 지표 목록 및 점수화
각 지표를 **-2 ~ +2** 로 점수화한 뒤 가중 합산 → -100 ~ +100 정규화.

| 그룹 | 가중치 | 지표 | +2 (강세) | -2 (약세) |
|---|---|---|---|---|
| **추세** | 25% | MA 배열 (20/60/120) | 완전 정배열 | 완전 역배열 |
| | | 종가 vs MA20 | +5% 이상 | -5% 이하 |
| **모멘텀** | 25% | RSI(14) | 55~70 | 30 미만 또는 80 초과 |
| | | MACD(12,26,9) | 골든크로스 + 히스토그램 확대 | 데드크로스 + 히스토그램 확대 |
| **밴드 위치** | 20% | 볼린저 %B | 0.5~0.8 | 0.2 미만 |
| | | 밴드폭(스퀴즈) | 스퀴즈 후 상단 돌파 | 스퀴즈 후 하단 이탈 |
| | | 엔벨로프 위치 | 중심선 위, 상단 미도달 | 상단 이탈(과열) |
| **거래량** | 15% | 거래량 / 20일 평균 | 1.5배 이상 + 주가 상승 | 1.5배 이상 + 주가 하락 |
| | | OBV 추세 | 20일 상승 | 20일 하락 |
| **매크로** | 15% | VIX 또는 VKOSPI | 15 미만 또는 하락 전환 | 25 초과 + 상승 |
| | | 미 10년물 금리 추세 | 하락 | 급등 |
| | | USD/KRW (국내 종목만) | 안정/하락 | 급등 |
| | | 지수 대비 상대강도(RS) | 20일 초과수익 | 20일 열위 |

> **주의**: 위 가중치와 임계값은 **초기 제안값**입니다. 실제 서비스 오픈 전 유니버스 전체에 대해 3~5년 백테스트로 보정해야 합니다. 최적 파라미터는 검증 없이 확정할 수 없으므로 **확인이 필요합니다.**

### 5.2 구간 판정
| 점수 | 구간 | 색상 |
|---|---|---|
| +40 ~ +100 | 매수 | 레드(진함) |
| +15 ~ +40 | 매수 우위 | 레드(연함) |
| -15 ~ +15 | 중립 | 그레이 |
| -40 ~ -15 | 매도 우위 | 블루(연함) |
| -100 ~ -40 | 매도 | 블루(진함) |

### 5.3 신뢰도 표기
데이터 부족(상장 1년 미만), 거래정지, 지표 결측 시 → 판정 대신 **"판정 불가"** 표시. 억지 판정 금지.

---

## 6. 데이터 소스

| 구분 | 후보 | 비고 |
|---|---|---|
| 국내 시세 | KRX 정보데이터시스템 OPEN API | 공식. **제공 항목·호출 한도는 확인이 필요합니다.** |
| 국내 시세 | 한국투자증권 KIS Developers | REST/WebSocket. 실계좌 개설 필요 |
| 해외 시세 | Alpha Vantage / Finnhub / Twelve Data / Polygon | 무료 티어 한도는 수시 변동 → **확인이 필요합니다.** |
| 해외 시세 | yfinance | 비공식 스크래핑. **상업 서비스에는 부적합** — 약관 위반 소지 |
| VIX | CBOE / 위 시세 API의 지수 심볼 | |
| VKOSPI | KRX | |
| 금리·환율 | FRED API (미 연준) / 한국은행 ECOS API | 둘 다 무료 키 발급 |
| CNN Fear & Greed | 공식 공개 API 없음 | **직접 산출하거나 VIX로 대체 필요 — 확인이 필요합니다.** |

**권고**: 1차는 **일봉 종가 데이터만** 사용. 실시간 시세는 비용·규제·구현 난이도가 모두 급증하므로 3차 이후 검토.

---

## 7. 기술 스택 (1인 개발 기준)

### 7.1 선택
| 레이어 | 기술 | 선정 이유 |
|---|---|---|
| 프론트엔드 | **Next.js 15 (App Router) + TypeScript** | SSR로 SEO 확보(종목 페이지가 검색 유입 채널), 프론트/BFF 통합 |
| 스타일 | **Tailwind CSS + shadcn/ui** | 컴포넌트 직접 작성 최소화 |
| 상태/데이터 | **TanStack Query** | 캐싱·재검증 자동화 |
| 차트 | **lightweight-charts** | 금융 차트 특화, 45KB, 오버레이 자유 |
| DB / 인증 | **Supabase (PostgreSQL + Auth)** | 이미 사용 경험 보유(Travel AI). 2차 로그인 즉시 도입 가능 |
| 배치/분석 | **Python 3.12 + pandas + pandas-ta** | 지표 계산은 Python이 압도적으로 빠르게 구현됨 |
| 배치 실행 | GitHub Actions cron 또는 Cloudtype/Fly.io | 1차는 GitHub Actions로 충분(무료) |
| 배포 | Vercel(웹) + Supabase(DB) | |
| 결제(2차) | 포트원(PortOne) 또는 토스페이먼츠 | 국내 정기결제 |

### 7.2 왜 이 구조인가 (서버 관점 설명)

**핵심 아이디어: "요청 시점에 계산하지 않는다."**

사용자가 종목을 검색할 때마다 외부 API를 호출하고 지표를 계산하면 다음 문제가 생깁니다.
- 응답이 2~5초 걸림 (외부 API 지연 + 계산)
- 외부 API 호출 한도를 사용자 트래픽이 직접 소모 → 트래픽 늘면 바로 터짐
- 같은 종목을 100명이 봐도 100번 계산 (완전한 낭비)

그래서 **읽기와 쓰기를 분리**합니다.

```
[매일 장 마감 후 배치 — 쓰기 경로]
외부 시세 API
   ↓ (Python, 하루 1회, 약 700종목)
일봉 데이터 저장 (daily_prices)
   ↓
지표 계산 (MA/BB/ENV/RSI/MACD/OBV)
   ↓
매크로 지표 수집 (VIX/VKOSPI/금리/환율)
   ↓
스코어링 → 구간 판정
   ↓
결과 저장 (daily_signals)  ← 여기까지 사용자와 무관하게 완료

[사용자 요청 — 읽기 경로]
브라우저 → Next.js → Postgres SELECT 1건
                     ↓
                  30~80ms 응답
```

이렇게 하면 사용자 요청은 **인덱스 걸린 테이블에서 행 하나 읽는 것**이 전부입니다. 트래픽이 10배 늘어도 DB 읽기만 늘어날 뿐 외부 API 호출은 그대로입니다.

**캐싱 계층**
1. **CDN 캐시(Vercel)**: 종목 상세 페이지는 하루에 한 번만 바뀌므로 `s-maxage=3600, stale-while-revalidate=86400` 설정. 인기 종목은 서버까지 오지도 않음.
2. **애플리케이션 캐시**: 검색 자동완성용 종목 마스터(약 700건)는 서버 메모리에 상주. DB 조회 불필요.
3. **DB 인덱스**: `daily_signals (ticker, trade_date DESC)` 복합 인덱스. 최신 1건 조회가 O(log n).

**레이트리밋**
- 검색 API: IP당 분당 60회 (자동완성이라 호출이 잦음)
- 상세 API: IP당 분당 20회
- Vercel Edge Middleware 또는 Upstash Redis로 구현

**배치 실패 대응 (중요)**
배치가 실패하면 그날 데이터가 통째로 비게 됩니다. 그래서:
- 종목 단위 재시도 (전체 롤백 아님)
- 마지막 성공 날짜를 화면에 명시: "2026-08-15 종가 기준"
- 2일 이상 갱신 실패 시 관리자 알림(이메일/슬랙)
- 사용자에게는 옛 데이터라도 **날짜와 함께** 보여주는 편이 빈 화면보다 낫습니다.

---

## 8. DB 스키마 (PostgreSQL)

```sql
-- 종목 마스터
CREATE TABLE symbols (
  ticker        TEXT PRIMARY KEY,          -- '005930', 'AAPL'
  market        TEXT NOT NULL,             -- KOSPI | KOSDAQ | NASDAQ | NYSE
  name_ko       TEXT,
  name_en       TEXT,
  universe      TEXT[] NOT NULL,           -- {KOSPI200}, {NASDAQ100,SP500}
  currency      TEXT NOT NULL,             -- KRW | USD
  is_active     BOOLEAN DEFAULT TRUE,
  updated_at    TIMESTAMPTZ DEFAULT now()
);
CREATE INDEX idx_symbols_search ON symbols
  USING gin (to_tsvector('simple', coalesce(name_ko,'') || ' ' || coalesce(name_en,'') || ' ' || ticker));

-- 일봉
CREATE TABLE daily_prices (
  ticker      TEXT REFERENCES symbols(ticker),
  trade_date  DATE NOT NULL,
  open        NUMERIC(18,4),
  high        NUMERIC(18,4),
  low         NUMERIC(18,4),
  close       NUMERIC(18,4),
  volume      BIGINT,
  PRIMARY KEY (ticker, trade_date)
);

-- 계산된 지표
CREATE TABLE daily_indicators (
  ticker      TEXT,
  trade_date  DATE,
  ma20 NUMERIC, ma60 NUMERIC, ma120 NUMERIC,
  bb_upper NUMERIC, bb_mid NUMERIC, bb_lower NUMERIC,
  bb_percent_b NUMERIC, bb_width NUMERIC,
  env_upper NUMERIC, env_lower NUMERIC,
  rsi14 NUMERIC,
  macd NUMERIC, macd_signal NUMERIC, macd_hist NUMERIC,
  vol_ratio20 NUMERIC, obv BIGINT,
  PRIMARY KEY (ticker, trade_date)
);

-- 매크로 (종목 무관, 날짜 단위)
CREATE TABLE daily_macro (
  trade_date  DATE PRIMARY KEY,
  vix NUMERIC, vkospi NUMERIC,
  us10y NUMERIC, dxy NUMERIC, usdkrw NUMERIC,
  kospi_close NUMERIC, spx_close NUMERIC
);

-- 최종 판정
CREATE TABLE daily_signals (
  ticker        TEXT,
  trade_date    DATE,
  score         SMALLINT NOT NULL,         -- -100 ~ +100
  zone          TEXT NOT NULL,             -- BUY|BUY_LEAN|NEUTRAL|SELL_LEAN|SELL|UNAVAILABLE
  summary       TEXT,
  breakdown     JSONB,                     -- {"trend":18,"momentum":12,...}
  tags          TEXT[],
  PRIMARY KEY (ticker, trade_date)
);
CREATE INDEX idx_signals_latest ON daily_signals (ticker, trade_date DESC);

-- 2차: 사용자 / 즐겨찾기 / 조회로그
CREATE TABLE favorites (
  user_id   UUID REFERENCES auth.users(id),
  ticker    TEXT REFERENCES symbols(ticker),
  added_at  TIMESTAMPTZ DEFAULT now(),
  PRIMARY KEY (user_id, ticker)
);

CREATE TABLE view_quota (
  subject_key TEXT,                        -- user_id 또는 device 해시
  quota_date  DATE,
  tickers     TEXT[] DEFAULT '{}',
  view_count  SMALLINT DEFAULT 0,
  PRIMARY KEY (subject_key, quota_date)
);
```

---

## 9. API 명세 (1차)

| Method | Path | 설명 | 응답 |
|---|---|---|---|
| GET | `/api/search?q=삼성` | 자동완성 | `[{ticker, name, market, price, changePct}]` |
| GET | `/api/stock/:ticker` | 종목 헤더 + 시그널 | `{symbol, price, signal}` |
| GET | `/api/stock/:ticker/chart?range=6M` | OHLCV + 지표 시계열 | `{candles[], bb[], env[], ma[], volume[]}` |
| GET | `/api/macro` | 당일 매크로 | `{vix, vkospi, us10y, usdkrw, asOf}` |

**응답 예시 — `/api/stock/005930`**
```json
{
  "symbol": { "ticker": "005930", "nameKo": "삼성전자", "market": "KOSPI", "currency": "KRW" },
  "price": { "close": 78500, "change": 1200, "changePct": 1.55, "asOf": "2026-08-14" },
  "signal": {
    "score": 42,
    "zone": "BUY",
    "summary": "20일선 위 정배열이 유지되고 거래량이 평균의 2.1배로 늘었습니다. 다만 VIX가 상승 중이라 시장 전체 부담은 남아 있습니다.",
    "breakdown": { "trend": 20, "momentum": 14, "band": 8, "volume": 12, "macro": -12 },
    "tags": ["정배열", "%B 0.82", "거래량 2.1배", "VIX 상승"],
    "asOf": "2026-08-14"
  }
}
```

---

## 10. 유료화 정책 (2차)

| | 무료 | 스탠다드 |
|---|---|---|
| 일일 종목 조회 | 5종목 | 무제한 |
| 즐겨찾기 | 5종목 | 50종목 |
| 지표 상세 근거 | 요약만 | 전체 |
| 과거 시그널 이력 | X | 1년 |
| 알림(3차) | X | O |
| 가격 | 0원 | **월 구독 — 금액 미확정, 확인이 필요합니다.** |

가격은 경쟁 서비스 조사 후 결정 필요. 초기에는 얼리버드 할인 + 연간 결제 유도 권장.

---

## 11. 법적 검토 (반드시 확인 필요)

> 이 항목은 서비스 오픈 전 **반드시 전문가 확인이 필요합니다.** 아래는 검토가 필요한 지점을 정리한 것이며 법률 자문이 아닙니다.

1. **유사투자자문업 신고**
   자본시장법상, 불특정 다수를 대상으로 **대가를 받고** 투자 판단에 관한 조언을 제공하는 경우 금융위원회 신고 대상이 될 수 있습니다. 2차(유료화) 진입 시점에 해당 여부를 반드시 확인해야 합니다. **확인이 필요합니다.**

2. **표현 설계**
   - "매수하세요" ❌ → "기술적 지표 기준 매수 구간" ⭕
   - "수익률 보장" 류 표현 전면 금지
   - 모든 판정 화면에 면책 문구 상시 노출

3. **시세 데이터 재배포**
   거래소 시세는 재배포 계약이 필요한 경우가 많습니다. 특히 **유료 서비스에서의 시세 제공**은 별도 계약 대상일 수 있습니다. **확인이 필요합니다.**

4. **개인정보**
   2차 로그인 도입 시 개인정보처리방침·이용약관 필수. 조회 로그의 IP 저장 시 해시 처리 권장.

---

## 12. 개발 순서 (1차 기준 체크리스트)

```
Week 1  DB 스키마 + 종목 마스터 적재(700종목) + 시세 API 키 발급/검증
Week 2  Python 배치: 일봉 수집 → 지표 계산 → DB 적재 (수동 실행)
Week 3  스코어링 엔진 + 구간 판정 + 3년 백테스트로 가중치 1차 보정
Week 4  Next.js 프로젝트 세팅 + 검색 API + 자동완성 UI
Week 5  종목 상세: 헤더 + 시그널 게이지 + 분석 카드
Week 6  차트(lightweight-charts) + BB/ENV/거래량 오버레이
Week 7  지표 패널 + 매크로 카드 + 즐겨찾기(localStorage)
Week 8  반응형/에러상태/면책문구 + 배치 자동화(cron) + 배포
```

---

## 13. 리스크 및 확인 필요 목록

| 항목 | 상태 |
|---|---|
| 시세 API 최종 선정 및 상업적 이용 가능 여부 | **확인이 필요합니다** |
| 각 API 무료 티어 호출 한도 | **확인이 필요합니다** |
| CNN Fear & Greed 대체 지표 | **확인이 필요합니다** (VIX 대체 권장) |
| 스코어링 가중치 최적값 | 백테스트 후 확정 — 현재는 제안값 |
| 유사투자자문업 신고 대상 여부 | **확인이 필요합니다** |
| 시세 재배포 라이선스 | **확인이 필요합니다** |
| 구독 가격 | **확인이 필요합니다** |

---

## 부록 A. 지표 계산식 요약

| 지표 | 계산식 |
|---|---|
| MA(n) | 최근 n일 종가 단순평균 |
| 볼린저밴드 | 중심 = MA20, 상/하단 = MA20 ± (2 × 20일 표준편차) |
| %B | (종가 − 하단) / (상단 − 하단) |
| 밴드폭 | (상단 − 하단) / 중심 |
| 엔벨로프 | MA20 × (1 ± 0.10) |
| RSI(14) | 100 − 100 / (1 + 14일 평균상승폭 / 14일 평균하락폭) |
| MACD | EMA12 − EMA26, 시그널 = MACD의 EMA9 |
| 거래량비 | 당일 거래량 / 20일 평균 거래량 |
| OBV | 종가 상승일 +거래량, 하락일 −거래량 누적 |
| 상대강도(RS) | 종목 20일 수익률 − 지수 20일 수익률 |
